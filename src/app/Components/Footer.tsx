import React from "react"
// import { useStaticQuery, graphql } from "gatsby"
// import PageContent from "./PageContent"

const Footer = () => {
  // const { wpPage } = useStaticQuery(
  //   graphql`
  //     query {
  //       wpPage(slug: { eq: "footer" }) {
  //         ...PageFragment
  //       }
  //     }
  //   `
  // )
  return (
    <h1>footer</h1>
  )
}

export default Footer
